#!/usr/bin/env bash

# m4_ignore(
echo "This is just a parsing library template, not the library - pass this file to 'argbash' to fix this." >&2
exit 11  #)Created by argbash-init v2.11.0
# ARG_OPTIONAL_SINGLE([timeLimit],,[time limit for each solve],[1])
# ARG_OPTIONAL_SINGLE([nSolve],,[number of times to call solver],[3])
# ARG_OPTIONAL_SINGLE([radius],,[major radius of torus],[2.0])
# ARG_OPTIONAL_SINGLE([tuberadius],,[tube radius of torus],[0.5])
# ARG_POSITIONAL_SINGLE([NUM],[number of points in data])
# ARGBASH_SET_DELIM([ =])
# ARG_OPTION_STACKING([getopt])
# ARG_RESTRICT_VALUES([no-local-options])
# ARG_DEFAULTS_POS
# ARG_HELP([Run experiments on solid torus.])
# ARGBASH_GO
